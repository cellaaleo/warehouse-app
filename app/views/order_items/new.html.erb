<h1>Novo item para o pedido <%= @order.code %></h1>

<%# form_with(model: @order_item)--> [[como no create tb precisaremos do order_id, vamos passar um array]] do |f|%>
<%= form_with(model: [@order, @order_item]) do |f| %>
  <div>
    <%= f.label  :product_model_id, 'Produto' %>
    <%= f.collection_select :product_model_id, @products, :id, :name %>
  </div>
  <div>
    <%= f.label  :quantity, 'Quantidade' %>
    <%= f.number_field :quantity %>
  </div>
  <%= f.submit "Gravar" %>
<% end %>